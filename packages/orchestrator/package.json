{
  "name": "@meta-chat/orchestrator",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest --config vitest.config.ts --run --coverage"
  },
  "dependencies": {
    "@meta-chat/shared": "*",
    "@meta-chat/events": "*",
    "@meta-chat/database": "*",
    "@meta-chat/llm": "*",
    "@meta-chat/rag": "*",
    "amqplib": "^0.10.4"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "typescript": "^5.4.0"
  }
}
